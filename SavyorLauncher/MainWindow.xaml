<Window x:Class="SavyorLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Savyor Launcher" 
        Height="300" 
        Width="500"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.45"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Window.Resources>
        <!-- Custom Progress Bar Style -->
        <Style x:Key="ModernProgressBar" TargetType="ProgressBar">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid Background="#1e293b">
                            <Border CornerRadius="4" Background="#1e293b" BorderBrush="#334155" BorderThickness="1"/>
                            <Border CornerRadius="4" HorizontalAlignment="Left" Name="PART_Indicator">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#8b5cf6" Offset="0"/>
                                        <GradientStop Color="#3b82f6" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <Border.Effect>
                                    <DropShadowEffect Color="#8b5cf6" BlurRadius="8" ShadowDepth="0" Opacity="0.5"/>
                                </Border.Effect>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Border CornerRadius="12" BorderBrush="#1e293b" BorderThickness="1.5">
        <Border.Background>
            <RadialGradientBrush Center="0.5,0.4" RadiusX="0.95" RadiusY="0.95">
                <GradientStop Color="#16143c" Offset="0"/>
                <GradientStop Color="#020308" Offset="1"/>
            </RadialGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="3" Opacity="0.6"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Grid Grid.Row="0" Background="#bb090d16" MouseDown="TitleBar_MouseDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="32"/>
                </Grid.ColumnDefinitions>
                
                <!-- Logo & Title -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                    <Ellipse Width="10" Height="10" Fill="#8b5cf6"/>
                    <TextBlock Text="SAVYOR" Foreground="#94a3b8" FontWeight="SemiBold" FontSize="11" Margin="8,0,0,0"/>
                    <TextBlock Text="| Launcher" Foreground="#475569" FontSize="10" Margin="6,0,0,0"/>
                </StackPanel>

                <!-- Close Button -->
                <Button Grid.Column="1" Background="Transparent" BorderThickness="0" Foreground="#64748b" Content="✕" FontSize="11" Click="CloseButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Name="bg" Background="Transparent" CornerRadius="0,12,0,0">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="bg" Property="Background" Value="#ef4444"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- Main Content Area -->
            <Grid Grid.Row="1" Margin="30,20,30,30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header/Branding -->
                <TextBlock Grid.Row="0" Text="Savyor Desktop" Foreground="White" FontSize="20" FontWeight="Bold"/>
                <TextBlock Grid.Row="1" Text="Preparing your workspace environment..." Foreground="#64748b" FontSize="12" Margin="0,4,0,0"/>

                <!-- Status details -->
                <Grid Grid.Row="2" Margin="0,45,0,8">
                    <TextBlock x:Name="StatusBox" Text="Initializing system..." Foreground="#e2e8f0" FontSize="11" HorizontalAlignment="Left"/>
                    <TextBlock x:Name="ProgressPercentage" Text="0%" Foreground="#8b5cf6" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                </Grid>

                <!-- Progress Bar -->
                <ProgressBar Grid.Row="3" x:Name="ProgressBox" Height="8" Style="{StaticResource ModernProgressBar}" Minimum="0" Maximum="100" Value="0"/>

                <!-- Version labels -->
                <Grid Grid.Row="4" Margin="0,15,0,0">
                    <TextBlock x:Name="LauncherVersionLabel" Text="Launcher: v1.0.0" Foreground="#475569" FontSize="10" HorizontalAlignment="Left"/>
                    <TextBlock x:Name="AppVersionLabel" Text="App: v0.0.0" Foreground="#475569" FontSize="10" HorizontalAlignment="Right"/>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
